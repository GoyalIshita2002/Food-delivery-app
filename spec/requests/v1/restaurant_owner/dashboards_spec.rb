require 'swagger_helper'

RSpec.describe 'v1/restaurant_owner/dashboards', type: :request do

  path '/v1/restaurant_owner/total_analysts' do

    get('analysts dashboard') do
      response(200, 'successful') do

        after do |example|
          example.metadata[:response][:content] = {
            'application/json' => {
              example: JSON.parse(response.body, symbolize_names: true)
            }
          }
        end
        run_test!
      end
    end
  end

  path '/v1/restaurant_owner/weekly_earning' do

    get('weekly_update dashboard') do
      response(200, 'successful') do

        after do |example|
          example.metadata[:response][:content] = {
            'application/json' => {
              example: JSON.parse(response.body, symbolize_names: true)
            }
          }
        end
        run_test!
      end
    end
  end
end
